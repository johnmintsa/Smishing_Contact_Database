Initiaize a Postgres database in PgAdmin, remember the password as we will need it to run this code.
In MyLocalPostgres Instance create a database and remember that name too. 
Create a ".env" file and populate it using this form:
DATABASE_HOSTNAME=localhost
DATABASE_PORT=5432
DATABASE_PASSWORD=<your_password>
database_name=<Database_name>
DATABASE_USERNAME=postgres
